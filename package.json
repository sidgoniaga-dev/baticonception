{
  "name": "baticonception",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build:css": "tailwindcss -i input.css -o tailwind.css --content './index.html,./src/app.jsx' --minify",
    "build:js": "node build.mjs",
    "build": "npm run build:css && npm run build:js",
    "postinstall": "npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwindcss": "^3.4.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
